#!/bin/sh
# Copyright (C) 2026 natserver
#
# 首次开机自动打开 WiFi（需求：2026-09-20，默认 SSID、无加密）。
#
# 背景：OpenWrt 的 board.d 在首次启动生成 /etc/config/wireless 时，
# 每个 wifi-device 都带 option disabled '1'（官方安全默认：不自动开无线）。
# AX1800 Pro（IPQ6000）有 2.4G / 5G 两个 radio，逐个删掉 disabled 标记
# 即可开启；SSID / 加密 / 信道保持默认（OpenWrt / none），用户连上后
# 自行在 LuCI「无线」页面配置密码。
#
# 执行时机：uci-defaults 在 S10boot 阶段运行，早于 network / wireless
# 服务启动，因此本步提交的配置会在同一次开机生效，无需再 wifi up。
#
# 注意：sysupgrade 保留配置升级到新固件时，uci-defaults 会再跑一次，
# 届时会重新打开 radio（即便升级前手动关过 WiFi），介意的话升级后
# 在 LuCI 里再关一次即可。

[ -s /etc/config/wireless ] || exit 0

i=0
while uci -q get "wireless.@wifi-device[$i]" >/dev/null 2>&1; do
	uci -q delete "wireless.@wifi-device[$i].disabled"
	i=$((i + 1))
done

uci -q commit wireless

exit 0
